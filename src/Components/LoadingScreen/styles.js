import { FontFamily } from '../../Theme';

export const root = { flex: 1, justifyContent: 'center' };

export const message = {
  fontFamily: FontFamily.OpenSans.regular,
  fontSize: 15,
  color: 'white',
  textAlign: 'center',
  paddingTop: 16
};
